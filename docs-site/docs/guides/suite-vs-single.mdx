---
title: Healing a suite vs a single test
description: Run the healer across the whole suite or target one failing spec.
---

# Healing a suite vs a single test

Run the healer across the whole suite or target one failing spec.

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- Whole-suite run + aggregate summary
- Single-file run (auto-captures the failure log)
- Feeding a pre-captured --log and --diff-base (the CI path)
:::
