---
title: Scope Guardrail
description: The engine only patches failing locators and wait conditions. It never mutates assertions or control flow.
---

# Scope Guardrail

The engine only patches failing locators and wait conditions. It never mutates assertions or control flow.

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- What is patched vs never touched (expect(), flow)
- Enforced at BOTH prompt and JSON-schema level
- Before/after diff showing assertion left untouched
- Why this matters for enterprise pipelines (deterministic safety)
- Every patch is human-reviewable
:::
