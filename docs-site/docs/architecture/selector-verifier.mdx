---
title: "Selector Verifier"
description: "Checks each patched selector against the live DOM so it resolves to exactly one element."
---

# Selector Verifier

Checks each patched selector against the live DOM so it resolves to exactly one element.

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- Node/Playwright helper
- 0 matches (hallucinated) / >1 (ambiguous) → revert + repatch
- Runs before a full test run
- Graceful skip conditions
:::
