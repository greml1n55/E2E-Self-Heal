---
title: Quickstart — GitHub Action
description: Add self-healing to your PRs in ~5 minutes. No local setup — it runs in the cloud and opens a patch PR.
---

# Quickstart — GitHub Action

Add self-healing to your PRs in ~5 minutes. No local setup — it runs in the cloud and opens a patch PR.

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- Prerequisites: a Playwright suite + NVIDIA NIM key secret
- Copy-paste workflow YAML (from ci/github-workflow.example.yml)
- Explain inputs: test-path, diff-base, app-url
- outcome output: passed/healed/unhealed
- What the patch PR looks like
- Scope guardrail reminder
:::
