---
title: heal vs review
description: Two outputs from one diagnosis engine: patch the test (heal) or advise the source fix (review).
---

# heal vs review

Two outputs from one diagnosis engine: patch the test (heal) or advise the source fix (review).

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- heal: patches selector/wait, re-runs until green
- review: inline PR comments, never edits the test
- When to pick which (intentional UI change vs source smell)
- Per-project / per-PR selection
- mode: review workflow pointer
:::
