---
title: "Quickstart — Local CLI"
description: "Run the same engine locally: pipx/uv install, point it at a failing spec, watch it heal."
---

# Quickstart — Local CLI

Run the same engine locally: pipx/uv install, point it at a failing spec, watch it heal.

:::note Draft — scaffolded page
This page is a stub. Planned contents:

- Install (pipx install ai-driven-e2e / uv tool install)
- Set E2E_HEALER_NVIDIA_API_KEY via .env
- Heal one test vs whole suite
- --dry-run preview
- Exit codes and --json summary
- Tabs: CLI command ↔ the JS/TS spec it scans
:::
